#include <stdio.h>
/**
 * main - Entry point
 * Return: Invariably 0 (Success)
 */

int main(void)
{
	char b;

	int d;

	b = 'a';
	d = 0;
	while 
		(d < 10) {
			putchar(d = '0');
			d++;
		}
	while
		(b <= 'f') {
			putchar(b);
			b++;
		}
	putchar('\n');
	return (0);
}
